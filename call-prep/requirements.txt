requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# Amazon blocks plain HTTP clients; the research module uses headless Chromium when
# this is installed. After `pip install -r requirements.txt` run: playwright install chromium
playwright>=1.45
